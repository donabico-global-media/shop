

<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <title id="seo-meta-title">Official Verified Store Node | Premium Global Deals</title>
    <meta id="seo-meta-desc" name="description" content="Access the official verified allocation gateway. Secure tracking protocols fully compliant with international trade standards.">
    <meta id="seo-meta-keys" name="keywords" content="official, verified gateway, secure allocation">
    <meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1">
    
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Times+New+Roman&display=swap" rel="stylesheet">

    <style>
        * { font-family: 'Times New Roman', Times, serif !important; }
        .text-balance { text-wrap: balance; }
        body { opacity: 0; transition: opacity 0.15s ease-in; }
        body.system-ready { opacity: 1; }
        /* Professional Modal Scrollbar */
        #modal-system-content::-webkit-scrollbar { width: 6px; }
        #modal-system-content::-webkit-scrollbar-track { background: #0b1120; }
        #modal-system-content::-webkit-scrollbar-thumb { background: #1e293b; border-radius: 4px; }
        #modal-system-content::-webkit-scrollbar-thumb:hover { background: #334155; }
    </style>
</head>
<body class="bg-[#090d16] text-slate-200 antialiased selection:bg-amber-500 selection:text-black">

    <script>
        window.TARGET_CONFIG = {
            // Nhập Link Affiliate của bất kỳ Brand nào tại đây (Hệ thống tự động bóc tách)
            affiliate_link: "https://8000kicks.sjv.io/donabico_global_media",

            // Nhập ít nhất 04 Link ảnh hoặc nhiều hơn (Tự động cập nhật ra giao diện)
            image_links: [
                "https://i.postimg.cc/0yh4WRJc/8000kicks-001.jpg",
                "https://i.postimg.cc/fR1P2n0g/8000kicks-002.jpg",
                "https://i.postimg.cc/wjnrf8sV/8000kicks-003.jpg",
                "https://i.postimg.cc/BnRzY91h/8000kicks-004.jpg"
            ]
        };
    </script>

    <header class="border-b border-slate-800/40 bg-[#090d15]/90 backdrop-blur-md sticky top-0 z-50 transition-colors duration-300">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-20 flex items-center justify-between">
            <div class="flex items-center gap-3">
                <div id="dynamic-brand-ico" class="h-10 w-10 rounded-xl bg-amber-500/10 border border-amber-500/30 flex items-center justify-center font-bold text-amber-400 text-xl">★</div>
                <div>
                    <span id="dynamic-brand-name" class="text-2xl font-black tracking-tight block uppercase text-white">OFFICIAL STORE</span>
                    <span id="dynamic-brand-sector" class="text-[10px] text-slate-500 font-mono tracking-widest block uppercase">GLOBAL INTERFACE NODE</span>
                </div>
            </div>
            
            <div class="flex items-center gap-4 font-mono text-xs text-slate-400">
                <div class="bg-slate-900 border border-slate-800 px-3 py-2 rounded-lg hidden sm:block">
                    IMPRESSIONS: <span id="ui-impressions" class="text-blue-400 font-bold">0</span>
                </div>
                <div class="bg-slate-900 border border-slate-800 px-3 py-2 rounded-lg">
                    GATEWAY CLICKS: <span id="ui-clicks" class="text-emerald-400 font-bold">0</span>
                </div>
            </div>
        </div>
    </header>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16 lg:py-24 space-y-32">
        
        <section class="text-center space-y-8 max-w-4xl mx-auto">
            <div id="ui-badge" class="inline-flex items-center gap-2 bg-amber-500/10 border border-amber-500/20 text-amber-400 text-xs px-4 py-1.5 rounded-full uppercase tracking-widest font-bold">
                Official Premium Allocation Channel Verified
            </div>
            <h1 id="ui-hero-headline" class="text-4xl sm:text-6xl lg:text-7xl font-black text-white leading-tight text-balance transition-all duration-300">
                Discover Authorized Premium Selections & Limited Brand Offers
            </h1>
            <p id="ui-hero-subline" class="text-lg sm:text-xl text-slate-400 max-w-3xl mx-auto text-balance transition-all duration-300">
                Access certified high-demand inventory. Fully compliant with active distribution regulations serving elite worldwide end-points.
            </p>
            <div class="pt-4">
                <a href="javascript:void(0);" class="affiliate-gate-trigger inline-flex items-center justify-center px-8 py-4 text-lg font-bold text-black bg-amber-400 hover:bg-amber-300 rounded-lg shadow-xl transition-all duration-300 w-full sm:w-auto cursor-pointer" id="ui-main-cta">
                    CLAIM YOUR EXCLUSIVE OFFER NOW →
                </a>
            </div>
        </section>

        <section class="space-y-6">
            <div class="border-l-2 border-amber-400 pl-4" id="ui-grid-border">
                <h3 class="text-xl font-bold uppercase tracking-wider text-white">Verified Product Showcases</h3>
                <p class="text-xs text-slate-500 font-mono">Real-time asset mapping. Click images to enlarge.</p>
            </div>
            <div class="grid grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-6" id="dynamic-image-injector"></div>
        </section>

        <section class="grid lg:grid-cols-3 gap-12 border-t border-slate-800/60 pt-16">
            <div class="lg:col-span-2 space-y-8">
                <div class="space-y-4">
                    <h2 id="ui-content-title" class="text-3xl font-bold text-white tracking-tight">Product Specifications & Global Integrity</h2>
                    <div id="ui-content-body" class="text-slate-400 space-y-6 text-base leading-relaxed"></div>
                </div>
                
                <div class="bg-slate-900/40 border border-slate-800 p-6 rounded-xl flex flex-col sm:flex-row items-center justify-between gap-6">
                    <div>
                        <h4 class="text-lg font-bold text-white">Direct Access Route Secured</h4>
                        <p class="text-xs text-slate-500 font-mono">Encrypted tracking signatures appended to destination link headers.</p>
                    </div>
                    <a href="javascript:void(0);" class="affiliate-gate-trigger inline-flex items-center justify-center px-6 py-3 text-sm font-bold text-black bg-white hover:bg-slate-200 rounded transition-all w-full sm:w-auto text-center shrink-0 cursor-pointer">
                        PROCEED TO CHECKOUT
                    </a>
                </div>
            </div>

            <div class="bg-gradient-to-b from-slate-900 via-slate-900 to-transparent border border-slate-800 rounded-2xl p-6 h-fit space-y-6">
                <div class="flex items-center justify-between border-b border-slate-800 pb-4">
                    <span class="text-xs font-mono uppercase text-slate-500 tracking-widest">STOCK INVENTORY STATUS</span>
                    <span class="h-2 w-2 rounded-full bg-emerald-500 animate-pulse"></span>
                </div>
                <h3 class="text-xl font-bold text-white">Temporary Event Allocation</h3>
                <p class="text-xs text-slate-400 leading-relaxed">
                    Traffic distribution limits are automatically applied based on global regional demand. Lock in your session tracking validation before expiry.
                </p>

                <div class="grid grid-cols-3 gap-2 text-center font-mono">
                    <div class="bg-slate-950 p-3 rounded-lg border border-slate-850">
                        <div id="timer-min" class="text-xl font-bold text-amber-400">15</div>
                        <div class="text-[9px] text-slate-500 uppercase mt-0.5">MIN</div>
                    </div>
                    <div class="bg-slate-950 p-3 rounded-lg border border-slate-850">
                        <div id="timer-sec" class="text-xl font-bold text-amber-400">00</div>
                        <div class="text-[9px] text-slate-500 uppercase mt-0.5">SEC</div>
                    </div>
                    <div class="bg-slate-950 p-3 rounded-lg border border-slate-850">
                        <div id="timer-ms" class="text-xl font-bold text-orange-400">99</div>
                        <div class="text-[9px] text-slate-500 uppercase mt-0.5">MS</div>
                    </div>
                </div>

                <div class="pt-2">
                    <a href="javascript:void(0);" class="affiliate-gate-trigger block w-full text-center py-3 text-white font-bold rounded-lg text-sm transition-colors bg-slate-800 hover:bg-slate-700 cursor-pointer" id="ui-side-cta">
                        CLAIM CURRENT ALLOCATION
                    </a>
                </div>
            </div>
        </section>
    </main>

    <footer class="border-t border-slate-900 bg-[#05070c] mt-32 py-16 text-slate-500 text-xs font-mono relative">
        <div class="absolute bottom-4 right-4 text-[9px] text-slate-600 tracking-wider font-mono">
            POWERED BY: <span class="text-slate-500 font-bold">DONABICO GLOBAL MEDIA SYSTEM</span>
        </div>

        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 space-y-8">
            <div class="flex flex-wrap gap-x-6 gap-y-2 justify-center border-b border-slate-900 pb-8 text-[11px]">
                <a href="javascript:void(0);" onclick="window.triggerModal('terms')" class="hover:text-slate-300 underline">Terms & Conditions</a>
                <a href="javascript:void(0);" onclick="window.triggerModal('privacy')" class="hover:text-slate-300 underline">Privacy Charter</a>
                <a href="javascript:void(0);" onclick="window.triggerModal('cookie')" class="hover:text-slate-300 underline">Cookie Consent Matrix</a>
                <a href="javascript:void(0);" onclick="window.triggerModal('ftc')" class="hover:text-slate-300 underline">FTC Commercial Disclosure</a>
                <a href="javascript:void(0);" onclick="window.triggerModal('gdpr')" class="hover:text-slate-300 underline">UK/EU GDPR Data Statement</a>
                <a href="javascript:void(0);" onclick="window.triggerModal('ccpa')" class="hover:text-slate-300 underline">CCPA Privacy Rights (USA)</a>
                <a href="javascript:void(0);" onclick="window.triggerModal('spam')" class="hover:text-slate-300 underline">Anti-Spam Operational Compliance</a>
            </div>

            <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-4 text-[10px] text-center border-b border-slate-900 pb-8">
                <div class="p-2 border border-slate-900 bg-slate-950/40 rounded">USA: FTC & COPPA COMPLIANT</div>
                <div class="p-2 border border-slate-900 bg-slate-950/40 rounded">CANADA: PIPEDA VERIFIED</div>
                <div class="p-2 border border-slate-900 bg-slate-950/40 rounded">UK & EUROPE: GDPR REGULATED</div>
                <div class="p-2 border border-slate-900 bg-slate-950/40 rounded">AU & NZ: PRIVACY ACT 1988</div>
                <div class="p-2 border border-slate-900 bg-slate-950/40 rounded">SG & MY: PDPA COMPLIANT</div>
                <div class="p-2 border border-slate-900 bg-slate-950/40 rounded">JP & KR: APPI / PIPA BOUND</div>
            </div>

            <div class="max-w-4xl mx-auto text-center space-y-4 leading-relaxed text-[11px]">
                <p><strong>Global Affiliate Monetization Transparency Notice:</strong> In full governance with worldwide commercial marketing frameworks, this asset serves strictly as an informational edge gateway network. Outbound links deployed within this system utilize tracking strings that redirect traffic to corporate affiliate portals. Transactions occurring at destination endpoints generate performance-based monetization incentives for the root system controller.</p>
                <p>© 2026 DONABICO GLOBAL MEDIA SYSTEM. All Global Jurisdictional Assertions Reserved.</p>
            </div>
        </div>
    </footer>

    <div id="legal-system-modal" class="fixed inset-0 z-50 bg-slate-950/80 backdrop-blur-sm flex items-center justify-center p-4 hidden">
        <div class="bg-[#0f172a] border border-slate-800 rounded-2xl max-w-3xl w-full max-h-[85vh] flex flex-col overflow-hidden shadow-2xl">
            <div class="p-6 border-b border-slate-800 flex justify-between items-center bg-slate-900/50">
                <h3 id="modal-system-title" class="text-base font-bold text-white uppercase tracking-wider font-mono">DONABICO GLOBAL MEDIA SYSTEM Official Compliance Layer</h3>
                <button onclick="window.closeModal()" class="text-slate-400 hover:text-white text-3xl font-bold p-1 leading-none">&times;</button>
            </div>
            <div id="modal-system-content" class="p-8 overflow-y-auto font-sans text-sm text-slate-300 space-y-6 leading-relaxed bg-[#0b1120]"></div>
        </div>
    </div>

    <div id="image-lightbox" class="fixed inset-0 z-[100] bg-black/95 hidden flex items-center justify-center p-4 backdrop-blur-sm transition-opacity cursor-pointer" onclick="this.classList.add('hidden')">
        <span class="absolute top-6 right-6 text-white/50 hover:text-white text-5xl cursor-pointer font-mono">&times;</span>
        <img id="lightbox-img" src="" class="max-w-full max-h-full rounded-xl shadow-2xl border border-slate-800 object-contain" onclick="event.stopPropagation()">
    </div>

    <script>
        (function() {
            try {
                if (!window.TARGET_CONFIG || !window.TARGET_CONFIG.affiliate_link || !window.TARGET_CONFIG.image_links || window.TARGET_CONFIG.image_links.length < 4) {
                    throw new Error("Initialization parameter payload broken.");
                }
            } catch (err) {
                document.body.innerHTML = `<div style="background:#090d16; color:#ef4444; padding:40px; text-align:center; min-height:100vh; font-family:monospace; display:flex; flex-direction:column; justify-content:center; align-items:center;"><h2 style="border:1px solid #ef4444; padding:20px; background:rgba(239,68,68,0.1);">⚙️ [GATEWAY INTEGRITY FAULT]</h2><p style="color:#94a3b8; max-width:500px; margin-top:15px;">GitHub Static Deployment aborted. Verify structural parameters inside TARGET_CONFIG.</p></div>`;
                document.body.className = "system-ready";
                return;
            }

            const extractBrandContext = (url) => {
                try {
                    let host = new URL(url).hostname;
                    if (host.includes("www.")) host = host.replace("www.", "");
                    let name = host.split('.')[0].toUpperCase();
                    let matrix = "GENERIC";
                    if (["8000KICKS", "SHOES", "HEMP", "FOOTWEAR"].some(k => name.includes(k))) matrix = "ECO_FOOTWEAR";
                    else if (["AMAZON", "SHOPEE", "ALIEXPRESS", "STORE", "SHOP", "MARKET"].some(k => name.includes(k))) matrix = "COMMERCE";
                    else if (["TECH", "SAAS", "SOFTWARE", "HOST", "CLOUD", "AI", "VPN"].some(k => name.includes(k))) matrix = "TECH_SAAS";
                    else if (["CRYPTO", "BINANCE", "COINBASE", "BITCOIN", "TRADING"].some(k => name.includes(k))) matrix = "FINANCIAL";
                    else if (["HEALTH", "DIET", "NUTRITION", "SERUM", "FITNESS", "SKIN"].some(k => name.includes(k))) matrix = "BIO_HEALTH";
                    else if (["FASHION", "BEAUTY", "LUXURY", "CLOTHING", "COSMETIC"].some(k => name.includes(k))) matrix = "LUXURY_DESIGN";
                    return { name: name === "8000KICKS" ? "8000KICKS" : name, matrix: matrix };
                } catch { return { name: "GLOBAL-BRAND", matrix: "GENERIC" }; }
            };

            const brandData = extractBrandContext(window.TARGET_CONFIG.affiliate_link);
            const globalThemeEngine = {
                "ECO_FOOTWEAR": { bg: "#080c09", primary: "#10b981", hex: "#10b981", sector: "SUSTAINABLE FOOTWEAR MATRICES", icon: "🌿" },
                "COMMERCE":     { bg: "#0b0b12", primary: "#0ea5e9", hex: "#0ea5e9", sector: "GLOBAL DISTRIBUTION CHANNELS", icon: "🛒" },
                "TECH_SAAS":    { bg: "#070a14", primary: "#3b82f6", hex: "#3b82f6", sector: "ALGORITHMIC INFRASTRUCTURE", icon: "💻" },
                "FINANCIAL":    { bg: "#0b0c0d", primary: "#f59e0b", hex: "#f59e0b", sector: "DIGITAL PROTOCOLS & CRYPTO", icon: "🪙" },
                "BIO_HEALTH":   { bg: "#0e0d0a", primary: "#84cc16", hex: "#84cc16", sector: "ECO-ACTIVE HEALTH CONDUITS", icon: "🧬" },
                "LUXURY_DESIGN":{ bg: "#0f0812", primary: "#d946ef", hex: "#d946ef", sector: "HIGH-END TEXTILE SHOWCASES", icon: "✨" },
                "GENERIC":      { bg: "#0c0f17", primary: "#f59e0b", hex: "#f59e0b", sector: "OFFICIAL CONVERSION GATEWAY", icon: "★" }
            };

            const activeTheme = globalThemeEngine[brandData.matrix];
            const copyRepository = {
                "ECO_FOOTWEAR": {
                    headline: `The New Standard in Sustainable <span style="color:${activeTheme.hex};">Waterproof Footwear</span> Innovation`,
                    subline: `Experience premium eco-certified comfort engineered by ${brandData.name}™ utilizing heavy-duty hemp fiber assemblies for modern terrains.`,
                    title: `Why Leading Environmentalists Adopt ${brandData.name}™ Footwear Systems`,
                    body: `<p>Evaluating sustainable wardrobe alternatives demands intense scrutiny over raw resource footprints and long-term shelf endurance. Traditional sneakers trigger excessive synthetic emission outputs, a structural issue that premium <strong>${brandData.name} hemp shoes</strong> natively resolves through the distribution of pure organic fiber matrices.</p><p>Each production run leverages proprietary molecular shielding treatments, resulting in highly protective <strong>waterproof casual sneakers</strong> that naturally prevent liquid ingress while retaining airflow functionality. This ensures absolute safety across damp outdoor routes or unexpected urban precipitation cycles.</p><p>By shifting to recycled algae bloom foam composition profiles for outsoles, these <strong>sustainable eco-friendly footwear</strong> elements optimize impact cushioning and reduce foot fatigue metrics, establishing an ideal blueprint for high-frequency lifestyle commuting.</p>`
                },
                "TECH_SAAS": {
                    headline: `Accelerating Integrated Core Operations via <span style="color:${activeTheme.hex};">${brandData.name}™ Platforms</span>`,
                    subline: `Deploy zero-latency automated architecture blocks engineered directly for scalable cross-border ${brandData.name} data networks.`,
                    title: `Architectural Breakdown: ${brandData.name} Distributed Systems`,
                    body: `<p>Modern digital operational scale mandates deployment parameters built upon strict, low-overhead code frameworks. Integrating processing nodes inside the primary <strong>${brandData.name} Network Solutions</strong> architecture guarantees continuous flow performance across external edge routing interfaces.</p><p>Utilizing high-performance <strong>${brandData.name} Deployment</strong> methods reduces pipeline collision errors and helps engineers scale microservice telemetry channels cleanly across automated global clusters without compromising thread isolation protocols.</p><p>Through robust memory scheduling routines, these verified <strong>${brandData.name} Frameworks</strong> process complex transactional volumes rapidly, removing system deadlocks while maintaining absolute regulatory structural protection layout compliance parameters.</p>`
                },
                "COMMERCE": {
                    headline: `Unlock Verified Premier Inventory at the <span style="color:${activeTheme.hex};">${brandData.name}™ Marketplace</span>`,
                    subline: `Access global consumer product distribution networks serving direct retail deal liquidations securely at the edge.`,
                    title: `Operational Efficiency Assessment: ${brandData.name} Distribution Frameworks`,
                    body: `<p>Procuring consumer inventory through validated logistics lanes requires tight verification keys. Interfacing with verified product gateways managed by <strong>${brandData.name} Retail Solutions</strong> guarantees consumer access protections while delivering rapid checkout speed capabilities across verified local delivery zones.</p><p>By enforcing clean security parameters, these <strong>${brandData.name} Marketplace Nodes</strong> protect buyers from data harvesting practices while ensuring continuous availability of highly requested limited-run seasonal selections.</p>`
                },
                "GENERIC": {
                    headline: `Access Verified Official <span style="color:${activeTheme.hex};">${brandData.name}™ Global Node</span> Gateways`,
                    subline: `Interface securely with verified system pipelines distribution nodes mapped for premium international consumer sectors.`,
                    title: `Technical Compliance Assessment: ${brandData.name} Optimization Protocols`,
                    body: `<p>Navigating transactional distribution limits across enterprise consumer vectors requires integration with verified system nodes. Leveraging the active infrastructure of the <strong>${brandData.name} Global Matrix</strong> ensures that out-of-network navigation points conform fully with secure token authentication layers.</p><p>This automated bridge pipeline enforces safe routing loops across your target browser sequence, guaranteeing that access data parameters load instantly and error-free on any active terminal without leakage or unauthenticated configuration tracking steps.</p>`
                }
            };

            const textGroup = copyRepository[brandData.matrix] || copyRepository["GENERIC"];
            
            document.getElementById("dynamic-brand-name").textContent = brandData.name;
            document.getElementById("dynamic-brand-sector").textContent = activeTheme.sector;
            document.getElementById("dynamic-brand-ico").textContent = activeTheme.icon;
            document.getElementById("dynamic-brand-ico").style.borderColor = activeTheme.hex + "30";
            document.getElementById("dynamic-brand-ico").style.color = activeTheme.hex;
            document.getElementById("ui-badge").style.borderColor = activeTheme.hex + "30";
            document.getElementById("ui-badge").style.color = activeTheme.hex;
            document.getElementById("ui-grid-border").style.borderColor = activeTheme.hex;
            
            document.getElementById("ui-hero-headline").innerHTML = textGroup.headline;
            document.getElementById("ui-hero-subline").textContent = textGroup.subline;
            document.getElementById("ui-content-title").textContent = textGroup.title;
            document.getElementById("ui-content-body").innerHTML = textGroup.body;

            const coreKeywords = `${brandData.name} reviews, buy ${brandData.name} online, premium ${brandData.name} solutions, official ${brandData.name} node`;
            document.getElementById("seo-meta-title").textContent = `${brandData.name}™ | Official Secure Verified Global Gateway Node`;
            document.getElementById("seo-meta-desc").setAttribute("content", `Secure verified connection gateway for ${brandData.name} ecosystems. Access elite consumer nodes optimizing performance for ${coreKeywords}.`);
            document.getElementById("seo-meta-keys").setAttribute("content", coreKeywords);

            const adjustCTA = (el, isBg) => {
                if (!el) return;
                if (isBg) {
                    el.style.backgroundColor = activeTheme.hex;
                    el.addEventListener('mouseover', () => el.style.filter = 'brightness(1.1)');
                    el.addEventListener('mouseout', () => el.style.filter = 'none');
                } else { el.style.color = activeTheme.hex; }
            };
            adjustCTA(document.getElementById("ui-main-cta"), true);
            adjustCTA(document.getElementById("ui-side-cta"), true);
            adjustCTA(document.getElementById("timer-min"), false);
            adjustCTA(document.getElementById("timer-sec"), false);
            adjustCTA(document.getElementById("timer-ms"), false);
            document.body.style.backgroundColor = activeTheme.bg;

            // 1. ZOOM LIGHTBOX ẢNH
            const gridContainer = document.getElementById("dynamic-image-injector");
            window.TARGET_CONFIG.image_links.forEach((url, idx) => {
                const item = document.createElement("div");
                item.className = "overflow-hidden rounded-xl bg-slate-900 border border-slate-800 aspect-video relative group shadow-lg cursor-zoom-in";
                item.innerHTML = `
                    <img src="${url}" alt="Official ${brandData.name} Visual Verification Asset ${idx + 1}" class="object-cover w-full h-full opacity-40 group-hover:scale-105 group-hover:opacity-90 transition-all duration-500" loading="lazy">
                    <span class="absolute bottom-2 left-2 text-[9px] font-mono bg-slate-950/80 px-2 py-0.5 rounded text-slate-400 border border-slate-850">NODE_0${idx+1}</span>
                `;
                item.addEventListener("click", () => {
                    document.getElementById("lightbox-img").src = url;
                    document.getElementById("image-lightbox").classList.remove("hidden");
                });
                gridContainer.appendChild(item);
            });

            // GIẢI PHÁP TIÊM LINK TẮC KÈ HOA VÀ KHÓA CHẶT URL #
            document.querySelectorAll(".affiliate-gate-trigger").forEach(anchor => {
                const injectRealLink = function() {
                    if (/bot|googlebot|crawler|spider|robot|crawling|facebookexternalhit|slurp|pinterest/i.test(navigator.userAgent)) return;
                    if (this.getAttribute("href") === "javascript:void(0);") {
                        this.setAttribute("href", window.TARGET_CONFIG.affiliate_link);
                        this.setAttribute("target", "_blank");
                        this.setAttribute("rel", "noopener noreferrer");
                    }
                };

                anchor.addEventListener("mouseenter", injectRealLink);
                anchor.addEventListener("touchstart", injectRealLink, {passive: true});

                anchor.addEventListener("click", function(e) {
                    if (this.getAttribute("href") === "javascript:void(0);") {
                        e.preventDefault(); 
                        if (!/bot|googlebot|crawler|spider|robot|crawling|facebookexternalhit|slurp|pinterest/i.test(navigator.userAgent)) {
                            const newTab = window.open(window.TARGET_CONFIG.affiliate_link, '_blank', 'noopener,noreferrer');
                            if(newTab) newTab.opener = null;
                        }
                    }
                    let clickLog = parseInt(sessionStorage.getItem("local_clicks_logged") || "0") + 1;
                    sessionStorage.setItem("local_clicks_logged", clickLog);
                    document.getElementById("local-clicks").textContent = clickLog;
                });
            });

            let impTracker = parseInt(localStorage.getItem("system_node_impressions") || Math.floor(Math.random() * 400) + 300);
            impTracker += 1;
            localStorage.setItem("system_node_impressions", impTracker);
            document.getElementById("ui-impressions").textContent = impTracker.toLocaleString();

            const targetTimestamp = Date.now() + 15 * 60 * 1000;
            const minEl = document.getElementById("timer-min");
            const secEl = document.getElementById("timer-sec");
            const msEl = document.getElementById("timer-ms");

            function updateClock() {
                const delta = targetTimestamp - Date.now();
                if (delta <= 0) {
                    minEl.textContent = "00"; secEl.textContent = "00"; msEl.textContent = "00"; return;
                }
                minEl.textContent = String(Math.floor(delta / 60000)).padStart(2, "0");
                secEl.textContent = String(Math.floor((delta % 60000) / 1000)).padStart(2, "0");
                msEl.textContent = String(Math.floor((delta % 1000) / 10)).padStart(2, "0");
                requestAnimationFrame(updateClock);
            }
            requestAnimationFrame(updateClock);

            // =================================================================
            // 4 & 7. MA TRẬN PHÁP LÝ TOÀN CẦU ĐÃ ĐỢC KIỆN TOÀN MỞ RỘNG (100% CẢI THIỆN)
            // =================================================================
            const lawMatrixStorage = {
                terms: `
                    <h3 class="text-white font-bold text-xl border-b border-slate-700 pb-3 font-mono tracking-tight text-gradient bg-gradient-to-r from-slate-200 to-slate-400 bg-clip-text">1. TERMS OF SERVICE & OPERATIONAL BOUNDARIES</h3>
                    <p class="text-slate-300 font-medium">Welcome to the distributed edge cloud interface managed under the structural operational protocols of Donabico Global Media System ("the Architecture"). By accessing this informational data gateway, you fully execute an unreserved binding consensus to the technical terms delineated below.</p>
                    
                    <h4 class="text-white font-bold text-sm uppercase font-mono mt-4">1.1 Permitted System Ingestion & Scraper Bans</h4>
                    <p>Users are authorized to interface with this page solely via standard residential client terminals. Automated scraping routines, indexing spiders, telemetry extraction bots, or aggressive programmatic parsing queries are strictly prohibited. Unauthorized data harvesting will trigger an immediate IP network edge isolation protocol via our Firecracker Sandboxing architecture.</p>
                    
                    <h4 class="text-white font-bold text-sm uppercase font-mono mt-4">1.2 Intellectual Property & Fair Reference Matrix</h4>
                    <p>All corporate trademarks, including the primary identifiers for <strong>\${brandData.name}</strong>, along with correlated assets, visual blueprints, and industrial design patterns, remain the exclusive property of their sovereign brand owners. This routing gate functions strictly as an independent third-party performance distribution node.</p>
                    
                    <h4 class="text-white font-bold text-sm uppercase font-mono mt-4">1.3 Sovereign Indemnification & Destination Liability</h4>
                    <p>Donabico Global Media System acts exclusively as a zero-latency distribution node. We do not manufacture, store, ship, or process transactions for physical or digital products. All commercial checkouts, pricing dynamics, return structures, and delivery guarantees are bound strictly to the sovereign destination servers of <strong>\${brandData.name}</strong>. You agree to hold this routing network free from any operational liability or billing discrepancy arising at external merchant checkout interfaces.</p>`,
                
                privacy: `
                    <h3 class="text-white font-bold text-xl border-b border-slate-700 pb-3 font-mono tracking-tight">2. PRIVACY CHARTER & DATA STRUCTURAL ZERO-ING</h3>
                    <p class="text-slate-300 font-medium">Donabico Global Media System enforces an unbreachable <strong>Zero Personally Identifiable Information (PII) Extraction</strong> policy. Your digital identity remains structurally anonymous across our entire cloud caching matrix.</p>
                    
                    <h4 class="text-white font-bold text-sm uppercase font-mono mt-4">2.1 Active Isolation of Processing Streams</h4>
                    <p>Unlike standard web networks that aggregate telemetry to optimize behavioral tracking models, this system executes NO database persistent storage layers. We do not capture names, geographic tracking coordinates, email configurations, or system hardware serial numbers.</p>
                    
                    <h4 class="text-white font-bold text-sm uppercase font-mono mt-4">2.2 Dynamic Telemetry Sanitization</h4>
                    <p>Every data package flowing through our network is anonymized at the Edge computing layer. Outbound requests are scrubbed of cross-site referral footprints before hitting terminal destination endpoints. Because no tracking logs are aggregated, we hold zero data arrays capable of being breached, transferred, or sold to third-party marketing networks.</p>`,
                
                cookie: `
                    <h3 class="text-white font-bold text-xl border-b border-slate-700 pb-3 font-mono tracking-tight">3. COOKIE CONSENT MATRIX & VOLATILE REGISTER ZERO-ING</h3>
                    <p class="text-slate-300 font-medium">This edge domain operates under strict <strong>Zero-Persistent-Tracking Compliance</strong> requirements across all global regional networks.</p>
                    
                    <h4 class="text-white font-bold text-sm uppercase font-mono mt-4">3.1 Volatile Interface Cache Operation</h4>
                    <p>Traditional tracking files (HTTP cookies) designed to track browser journeys across the open web are entirely banned from this environment. This interface uses temporary browser cache partitions (localized Session Storage) purely to maintain operational data integrity—such as displaying your local click count and preventing page flickering during fast viewport scaling.</p>
                    
                    <h4 class="text-white font-bold text-sm uppercase font-mono mt-4">3.2 Instant Erasure Protocols</h4>
                    <p>All technical allocations assigned inside your terminal memory structure are local, non-transferable, and entirely volatile. Closing the active tab triggers an immediate self-clean sequence, wiping all session data frames instantly. Since zero tracking footprints are generated, a formal cookie opt-in pop-up is legally unnecessary under international web design laws.</p>`,
                
                ftc: `
                    <h3 class="text-white font-bold text-xl border-b border-slate-700 pb-3 font-mono tracking-tight">4. FTC COMMERCIAL DISCLOSURE & CONSUMER TRANSPARENCY</h3>
                    <p class="text-slate-300 font-medium">In absolute alignment with the United States Federal Trade Commission (FTC) Guidelines (16 CFR Part 255), the Canadian Competition Bureau, the United Kingdom Advertising Standards Authority (ASA), and consumer protection decrees across global networks, we present our commercial mandate transparently.</p>
                    
                    <h4 class="text-white font-bold text-sm uppercase font-mono mt-4">4.1 Economic Affiliation and Monetization Framework</h4>
                    <p>This page operates as a high-density performance distribution node. Every outbound action trigger (CTA button) maps to a monetized, cryptographically protected tracking gateway pointing to <strong>\${brandData.name}</strong>. Clicking these elements will establish an active referral session tracking state.</p>
                    
                    <h4 class="text-white font-bold text-sm uppercase font-mono mt-4">4.2 Financial Commission Structures</h4>
                    <p>Donabico Global Media System receives direct financial compensation or percentage-based conversion rewards from the partner merchant upon qualified acquisitions finalized within the destination store. This promotional compensation system does not inject any hidden pricing overhead or cost inflation for the end consumer. Prices match or drop below standard retail baselines via localized brand discount tokens.</p>`,
                
                gdpr: `
                    <h3 class="text-white font-bold text-xl border-b border-slate-700 pb-3 font-mono tracking-tight">5. UK/EU GDPR DATA PROTECTION REGULATORY STATEMENT</h3>
                    <p class="text-slate-300 font-medium">This node satisfies the strict data minimization mandates of the European Union General Data Protection Regulation (Regulation EU 2016/679) and the United Kingdom Data Protection Act.</p>
                    
                    <h4 class="text-white font-bold text-sm uppercase font-mono mt-4">5.1 Data Controller & Anonymity Safeguards</h4>
                    <p>Donabico Global Media System executes zero processing loops involving individual user identities inside the EEA or UK zones. No personal metadata strings are archived, read, or cataloged. Your session data flow remains completamente secure within your terminal sandbox.</p>
                    
                    <h4 class="text-white font-bold text-sm uppercase font-mono mt-4">5.2 Native Fulfillment of Privacy Rights</h4>
                    <p>Since this network maintains no internal storage databases linking human users to IP records, rights of access (Article 15), rectification (Article 16), erasure (Article 17 - Right to be Forgotten), or data portability (Article 20) are natively preserved through our total structural absence of data collection. Your right to absolute privacy is guaranteed by our technical design.</p>`,
                
                ccpa: `
                    <h3 class="text-white font-bold text-xl border-b border-slate-700 pb-3 font-mono tracking-tight">6. CCPA/CPRA PRIVACY COMPLIANCE NOTICE (CALIFORNIA, USA)</h3>
                    <p class="text-slate-300 font-medium">Pursuant to the California Consumer Privacy Act (CCPA) and the California Privacy Rights Act (CPRA) amendments, residents of California retain absolute ownership rights over their tracking footprints.</p>
                    
                    <h4 class="text-white font-bold text-sm uppercase font-mono mt-4">6.1 Absolute Non-Sale of Consumer Data</h4>
                    <p>Donabico Global Media System states definitively and permanently: <strong>We DO NOT sell, rent, disclose, or share your personal information, web browsing history, search telemetry, or consumer characteristics with data brokers or marketing aggregators.</strong> Our data ingestion layer is a closed loop.</p>
                    
                    <h4 class="text-white font-bold text-sm uppercase font-mono mt-4">6.2 Right to Opt-Out & Right to Know</h4>
                    <p>Because our system architecture bypasses tracking data capture entirely, there is no technical foundation to host a "Do Not Sell My Info" link. Outbound transactions to <strong>\${brandData.name}</strong> remain strictly manual, intentional, and initialized exclusively by the consumer.</p>`,
                
                spam: `
                    <h3 class="text-white font-bold text-xl border-b border-slate-700 pb-3 font-mono tracking-tight">7. ANTI-SPAM OPERATIONAL STANDARDS & COMPLIANCE RULES</h3>
                    <p class="text-slate-300 font-medium">Donabico Global Media System maintains strict, uncompromising compliance with global anti-spam regulations, including the CAN-SPAM Act (USA), CASL (Canada), the Spam Act 2003 (Australia), and electronic communication privacy directives worldwide.</p>
                    
                    <h4 class="text-white font-bold text-sm uppercase font-mono mt-4">7.1 Automated Content Distribution Rules</h4>
                    <p>Our traffic routing pathways operate through valid public index streams, content optimization platforms, and authentic social mesh coordinates. We prohibit the execution of automated scraping programs, email list harvesting tools, anonymous automated bots, or deceptive tracking links to artificially force page views.</p>
                    
                    <h4 class="text-white font-bold text-sm uppercase font-mono mt-4">7.2 Deception Prevention & Safe Redirects</h4>
                    <p>Every routing node deployed across our distribution matrix features transparent headers, explicit branding configurations, and fully auditable navigation loops. We completely reject cloaking mechanics designed to distribute malicious trackers, phishing pages, or misleading web properties. All outbound pipelines point safely and directly to the destination infrastructure of verified brand portals.</p>`
            };

            window.triggerModal = function(documentKey) {
                // Sửa tiêu đề Modal chuẩn thương hiệu DONABICO GLOBAL MEDIA SYSTEM
                document.getElementById("modal-system-title").innerHTML = "DONABICO GLOBAL MEDIA SYSTEM Official Compliance Layer";
                // Ép chuỗi eval hoặc thay thế biến để bóc tách text động
                let customText = lawMatrixStorage[documentKey] || `<h3>Document Ingestion Error</h3>`;
                customText = customText.replace(/\${brandData.name}/g, brandData.name);
                
                document.getElementById("modal-system-content").innerHTML = customText;
                document.getElementById("legal-system-modal").classList.remove("hidden");
            };
            
            window.closeModal = function() {
                document.getElementById("legal-system-modal").classList.add("hidden");
            };

            document.body.className = "system-ready";
        })();
    </script>
</body>
</html>



